
  # Camera Scan Screen Design

  This is a code bundle for Camera Scan Screen Design. The original project is available at https://www.figma.com/design/sJxvRT2wurQtdRUKSV3ivF/Camera-Scan-Screen-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  